new
  Constructor

recognised
  Is the current system one for which we recognize a packaging system?

isInstalled
  Is the named package installed?

installPackage
  Install the named package.

removePackage
  Remove the named package.
